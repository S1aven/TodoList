import React from 'react';

const ControlButton = () => {
  return (
    <div>
      <div>
        <button>All</button>
        <button>Active</button>
        <button>Completed</button>
      </div>
    </div>
  );
};

export default ControlButton;